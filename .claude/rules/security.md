# Security Rules

Never commit secrets.

Never expose API keys to the frontend.

Never upload user archives without explicit configuration.

Do not log private conversation contents unnecessarily.

Treat imported files as untrusted input.

Keep cloud functionality disabled by default.

Review filesystem paths carefully to prevent path traversal.

Document security-sensitive architectural decisions.