# Prompts — 2026-09-08-03-milestone-2-chatgpt-importer

The instructions given during this session, verbatim and in order.

No secrets appeared in any prompt, so nothing has been redacted.

---

## Prompt 1 — session start

```
Yes start with next milestone, sure I will download mine current and have it imported into project temp local folder for testing
```

This established two things: proceed with Milestone 2, and the user will place
a real ChatGPT export in a local folder for testing. That produced `local/`,
git-ignored with a committed README, created before any importer code so a real
export could never be committed by accident.

## Prompt 2 — mid-session

```
Sorry my bad, tab got closed accidently, please continue
```

The session was interrupted and resumed. Nothing was lost, because the work was
committed and recorded as it went — which is the reason this protocol exists.
