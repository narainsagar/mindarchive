# Prompts — 2026-09-08-04-milestone-3-archive-browser-and-search

The instructions given during this session, verbatim and in order.

No secrets appeared in any prompt, so nothing has been redacted.

---

## Prompt 1 — session start

```
Lets start and proceed with next milestone if everything was done and updated accordingly throughout.
```

Checked first whether a real ChatGPT export had appeared in `local/`. It had
not — only the synthetic one from the previous session. Proceeded anyway, since
Milestone 3 reads from the archive on disk rather than from exports, so a parser
bug could not invalidate this work. The real-export verification stays
outstanding and is recorded in `PROJECT_STATE.md`.
